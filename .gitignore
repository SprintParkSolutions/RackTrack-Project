# dependencies — restore with `npm install`
node_modules/

# raw inputs: source photography, screen recordings, superseded assets.
# kept on disk, out of the repo (they were adding ~35 MB)
media/

# OS
.DS_Store
dist/
racktrack-site.zip

# agent scratch/verification files
_*.html
__*.html

.claude/
